pct exec 401 -- bash -c 'set -u; cat > /usr/local/bin/aimee.new; chmod 0755 /usr/local/bin/aimee.new; if ! head -c4 /usr/local/bin/aimee.new | grep -q ELF; then echo "NOT AN ELF - aborting"; rm -f /usr/local/bin/aimee.new; exit 1; fi; if ! /usr/local/bin/aimee.new --version 2>/dev/null | grep -q aimee; then echo "DOES NOT RUN - aborting"; rm -f /usr/local/bin/aimee.new; exit 1; fi; cp -a /usr/local/bin/aimee /usr/local/bin/aimee.prev; mv /usr/local/bin/aimee.new /usr/local/bin/aimee; echo "installed: $(/usr/local/bin/aimee --version)"; echo INSTALL_OK'
