/* Installs a recorded permissions provider. See delegate_permissions_stub.c for
 * what it does and does not stand in for. */
#ifndef AIMEE_TESTS_DELEGATE_PERMISSIONS_STUB_H
#define AIMEE_TESTS_DELEGATE_PERMISSIONS_STUB_H

void delegate_permissions_stub_install(void);

#endif
