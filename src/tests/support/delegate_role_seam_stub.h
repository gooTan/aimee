/* Registers the module's recorded role answers into the real delegate_role.c
 * seam. See delegate_role_seam_stub.c for what it does and does not stand in
 * for. */
#ifndef AIMEE_TESTS_DELEGATE_ROLE_SEAM_STUB_H
#define AIMEE_TESTS_DELEGATE_ROLE_SEAM_STUB_H

void delegate_role_seam_install(void);

#endif
